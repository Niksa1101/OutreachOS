"""Product modules.

One package per module, each owning its router, service, models, and schemas.
Modules are isolated: shared behaviour belongs in ``outreachos_backend.core``,
never imported sideways between modules.

V1 ships exactly one — ``video_composer``.
"""
