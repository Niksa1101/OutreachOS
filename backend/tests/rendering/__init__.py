"""Render-engine tests.

A package, like ``tests`` itself: without it this directory's ``test_config.py``
and ``tests/test_config.py`` collide on module basename under pytest's default
import mode.
"""
