# `video-composer/components/`

Module-specific UI, composed from `core/ui` and shadcn primitives, styled only
through design tokens.

Anything that turns out to be useful to a second module moves to `core/ui`
instead of being imported across the module boundary.
