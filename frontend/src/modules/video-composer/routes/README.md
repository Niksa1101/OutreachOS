# `video-composer/routes/`

Route components registered through `core/registry`.

Planned: `Campaigns` (P2), `CampaignDetail` with the split-view overlay editor
(P3), `RenderQueue` (P4), `Outputs` (P5).
