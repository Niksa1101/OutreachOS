# `video-composer/state/`

Zustand stores for **ephemeral UI state only** — editor drag interaction,
transient panel state, which handle is active.

Never server data. If it came from the API, it belongs in TanStack Query.
